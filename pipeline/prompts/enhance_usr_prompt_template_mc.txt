Validate the following multiple-choice question and its answer:

Original Question:
{question}

Parameters:
- **Subject**: {subject}
- **Subject Area**: {subject_area}
- **Topic**: {skill}
- **Learning Objectives**: 



**IMPORTANT: Do not ask any clarifying questions. Validate and correct the answer directly based on the provided parameters.**

**Instructions:**
1. Keep the question stem and multiple-choice options exactly as provided.
2. Verify if the marked correct answer is indeed correct.
3. If the current answer is incorrect, identify the correct answer and provide detailed justification.
4. Ensure the answer aligns with the learning objectives provided.
5. Keep the same Bloom's Taxonomy level and level number.
6. Each question must include:
   - The original question stem (unchanged)
   - The original four multiple-choice options (unchanged)
   - The validated correct answer (only the label, e.g., "A")
   - The same Bloom's Taxonomy level and level number
   - The same topic and skill_name
   - The same Subject and Subject Area
7. **Output only valid JSON responses** exactly as described in the system prompt.
8. **Mathematical Notation:** Use LaTeX inside MathJax delimiters for any mathematical expressions.
9. Make sure the response follows proper escape invalid sequences
10. Return the output as a **valid JSON string**, ready to be parsed with `JSON.parse()` or `json.loads()` in code.

### Required LaTeX Notation:
Use the following LaTeX formatting inside **MathJax delimiters** for proper rendering.
- **Exponents:** Use `x^{{2}}` (e.g., `\\( x^{{2}} \\)`). Complex: `x^{{2n + 1}}` (e.g., `\\( x^{{2n + 1}} \\)`). **Wrap expressions in MathJax delimiters:** Inline: `\\( v(t) = 3t^{{2}} - 12t + 9 \\)`, Block: `\\[ v(t) = 3t^{{2}} - 12t + 9 \\]`.
- **Fractions:** Use `\\frac{{a}}{{b}}` (e.g., `\\( \\frac{{1}}{{2}} \\)`).
- **Summation:** Use `\\sum_{{i=1}}^{{n}} x_i` (e.g., `\\( \\sum_{{i=1}}^{{n}} i \\)`).
- **Products:** Use `\\prod_{{i=1}}^{{n}} x_i` (e.g., `\\( \\prod_{{i=1}}^{{n}} i \\)`).
- **Square Roots:** Use `\\sqrt{{x}}` (e.g., `\\( \\sqrt{{4}} \\)`).
- **Derivatives:** Use `\\frac{{d}}{{dx}} f(x)` (e.g., `\\( \\frac{{d}}{{dx}} x^{{2}} = 2x \\)`).
- **Integrals:** Definite: `\\int_{{a}}^{{b}} f(x) \\,dx` (e.g., `\\( \\int_{{0}}^{{1}} x^{{2}} \\,dx \\)`). Indefinite: `\\int f(x) \\,dx` (e.g., `\\( \\int x^{{2}} \\,dx \\)`).
- **Limits:** Use `\\lim_{{x \\to \\infty}} \\frac{{1}}{{x}}` (e.g., `\\( \\lim_{{x \\to \\infty}} \\frac{{1}}{{x}} \\)`).
- **Greek Letters:** Use LaTeX symbols: `\\( \\alpha \\)`, `\\( \\beta \\)`, `\\( \\gamma \\)`, `\\( \\theta \\)`.
- **Matrices:**  
  ```latex
  \[
    \begin{{bmatrix}} a & b \\ c & d \end{{bmatrix}}
  \]  

