Generate exactly **{num_questions} hard and challenging college-level multiple-choice questions** for {subject} using the following parameters:
- **Subject**: {subject}
- **Subject Area**: {subject_area}
- **topic**: {skill}
- **learning_objectives**: 

{learning_objectives}

{sample_questions_section}

**IMPORTANT: Do not ask any clarifying questions. Generate questions directly based on the provided parameters.**

**Instructions:**
1. Ensure every question is highly challenging and directly related to the provided subject, topic, and skills.
2. **CRITICAL: Generate questions that cover ALL learning objectives in the learning objectives above. Each skill must be represented by at least one question.**
3. Questions must require Multi-Step Reasoning, Conceptual Depth, and Graphical or Experimental Analysis.
4. Ensure:
   - High mathematical rigor typical of AP college-level subjects.
   - High graphical/experimental complexity.
   - Equal focus on conceptual reasoning and quantitative problem-solving.
   - Coverage of all major subtopics within {learning_objectives}.
5. Each question must include:
   - A question stem.
   - Four multiple-choice options (labeled A, B, C, D) with consistent formatting.
   - The correct answer (only the label, e.g., "A").
   - Bloom's Taxonomy level assignment ("Applying", "Analyzing", "Evaluating") and level number.
   - topic 
   - skill_name concatenating bloom level and topic as "<level>-<Topic>" 
   - Subject 
   - Subject Area
   - Metadata including:
     * word_count
     * has_math_notation
     * requires_diagram
     * blooms_level
     * difficulty
6. If a diagram is required, set the `requires_diagram` flag to `true` and provide diagram instructions.
7. **Output only valid JSON responses** exactly as described in the system prompt.
8. **Mathematical Notation:** Use LaTeX inside MathJax delimiters for any mathematical expressions.
9. Make sure the response  with proper escape invalid sequences
10. Return the output as a **valid JSON string**, ready to be parsed with `JSON.parse()` or `json.loads()` in code. Example of correctly escaped LaTeX


### Required LaTeX Notation:
Use the following LaTeX formatting inside **MathJax delimiters** for proper rendering.
- **Exponents:** Use `x^{{2}}` (e.g., `\\( x^{{2}} \\)`). Complex: `x^{{2n + 1}}` (e.g., `\\( x^{{2n + 1}} \\)`). **Wrap expressions in MathJax delimiters:** Inline: `\\( v(t) = 3t^{{2}} - 12t + 9 \\)`, Block: `\\[ v(t) = 3t^{{2}} - 12t + 9 \\]`.
- **Fractions:** Use `\\frac{{a}}{{b}}` (e.g., `\\( \\frac{{1}}{{2}} \\)`).
- **Summation:** Use `\\sum_{{i=1}}^{{n}} x_i` (e.g., `\\( \\sum_{{i=1}}^{{n}} i \\)`).
- **Products:** Use `\\prod_{{i=1}}^{{n}} x_i` (e.g., `\\( \\prod_{{i=1}}^{{n}} i \\)`).
- **Square Roots:** Use `\\sqrt{{x}}` (e.g., `\\( \\sqrt{{4}} \\)`).
- **Derivatives:** Use `\\frac{{d}}{{dx}} f(x)` (e.g., `\\( \\frac{{d}}{{dx}} x^{{2}} = 2x \\)`).
- **Integrals:** Definite: `\\int_{{a}}^{{b}} f(x) \\,dx` (e.g., `\\( \\int_{{0}}^{{1}} x^{{2}} \\,dx \\)`). Indefinite: `\\int f(x) \\,dx` (e.g., `\\( \\int x^{{2}} \\,dx \\)`).
- **Limits:** Use `\\lim_{{x \\to \\infty}} \\frac{{1}}{{x}}` (e.g., `\\( \\lim_{{x \\to \\infty}} \\frac{{1}}{{x}} \\)`).
- **Greek Letters:** Use LaTeX symbols: `\\( \\alpha \\)`, `\\( \\beta \\)`, `\\( \\gamma \\)`, `\\( \\theta \\)`.
- **Matrices:**  
  ```latex
  \[
    \begin{{bmatrix}} a & b \\ c & d \end{{bmatrix}}
  \]  

